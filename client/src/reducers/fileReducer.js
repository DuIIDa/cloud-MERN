const initialState = {
    currentUser: {},
    isAuth: false
}

export default function fileReducer(state = initialState, action) {
    switch (action.type) {
    
        default:
            return state
    }
}